Вера Жуковская
Telegram - @verisola

Terminal. HW_2

1. Сделать папку dir_1

terminal mkdir dir_1

 2. Зайти в папку dir_1
cd dir_1/

 3. Создать папку inner_dir_1
mkdir inner_dir_1

 4. Посмотреть где ты находишься
 pwd

 5. Находясь в папке dir_1 создать пустой текстовый файл tf_1.txt
touch tf_1.tx

 6. Находясь в папке dir_1 через команду cat создать текстовый файл tf_2.txt со следующими строками:
- the first 1
- the second 2
- the third 3

cat > tf_2.txt
- the first 1
- the second 2
- the third 3
после каждой строки enter
в самом конце ctrl+d

 7. Зайти в папку inner_dir_1
cd inner_dir_1/

 8. Через cat сделать текстовый файл tf_3.txt  c любыми строками
cat > tf_3.txt
 beginnig
 process
 final
 после каждой строки enter
 в самом конце ctrl+d

 9. Через cat добавить в текстовый файл tf_3.txt строку “the second 2”
 cat >> tf_3.txt
 - the second 2
 enter
 ctrl+d

 10. Через cat добавить в текстовый файл tf_3.txt строку “the sec 2”
 cat >> tf_3.txt
 - the sec 2
 enter
 ctrl+d

 11. Через cat добавить в текстовый файл tf_2.txt строку “the sec 3”
cd ..
cat >> tf_2.txt
- the sec 3
enter
ctrl+d

# Тут нужно было обратиться к файлу по относительному пути: cat >> ../tf_2.txt
-если .. - в папке на шаг выше
-или папку, к-я нах-ся внутри действующей директории

b)Если не выходить из папки inner_dir_1:
⋊> ~/D/Q/H/2/d/inner_dir_1 cat >> ../tf_2.txt
- the sec 3
enter
ctrl+d

# в папку dir_1 на шаг выше

 12. Через cat добавить в текстовый файл tf_3.txt строку “the SeCoNd 2”
cat >> tf_3.txt
- the seCoNd 2
enter
ctrl+d

 13. Через cat добавить в текстовый файл tf_2.txt строку “the seConD 2”
cat >> ../tf_2.txt
- the seConD 2
enter
ctrl+d

 14. Сделать текстовый файл tf_4.txt в котором будет 15 строк.
yes '' | head -n 14 > tf_4.txt

 15. Сделать текстовый файл tF_5.txt в котором будет 13 строк.
yes '' | head -n 12 > tF_5.txt

 16. Вывести список всех файлов в папке.
 ls -la

 17. Выйти из папки inner_dir_1
cd ..

 18. Вывести содержимое файла tf_3.txt в терминал.
cat inner_dir_1/tf_3.txt

 19. Найти путь к файлу tf_4.txt
mdfind tf_4.txt (покажет и путь к этому файлу и файлу, содержащему название tf_4.txt)

realpath tf_4.txt (покажет только путь)
# это лучше. Но нужно быть осторожным с realpath: он может показать путь к несуществующему файлу.
# самое правильное решение этой задачи (на мой взгляд): find -name tf_4.txt | xargs realpath
# еще вариант: readlink -f $(find -name "tf_4.txt")

find ПУТЬ -name ИМЯ ФАЙЛА (поиск внутри действующей директории и по папкам ниже)

 20. Очистить файл tf_4.txt от содержимого без удаления самого файла.
echo -n > inner_dir_1/tf_4.txt

 21. Найти путь к файлам у которых есть  “tf” в названии.
find . -type f -name "*tf*"


 22. Найти путь к файлам у которых есть  “tf” в названии и буквы в любом регистре.
find . -type f -iname "*tf*"

 23. Найти строки в файлах где есть комбинация букв “sec” в текущей папке
grep sec *.*
# хорошо, только маска файла предполагает что вы ищите файлы с расширением. файлы без расширения не найдутся.

Эта команда лучше:     grep -r sec .
                       grep sec *   - в текущей папке

 24. Найти строки в файлах где есть комбинация букв “sec” в любом регистре в текущей папке
 grep sec -i *.*

Эта команда лучше:     grep -r -i sec .
                       grep -i sec *  - в текущей папке

 25. Найти строки в файлах где есть только комбинация букв “sec” в текущей папке
 grep -w "sec" *.*
# здесь подвох: ключ -w ищет вхождение слов, например 'one sec, please'. А нам нужно чтобы вся строка состояла только из одного слова 'sec'
# ну и маска ограничивает поиск только файлами с расширением

можно:
grep -rx "sec" .
grep -x "sec" *

 26. Найти строки в файлах где есть только комбинация букв “sec” в любом регистре в текущей папке
 grep -iw "sec" *.*
# флаг -w лишний, ведь нам нужно любое вхождение букв, включая 'second'

grep -rix "sec" .

 27. Найти строки в файлах где есть комбинация букв “second” в текущей папке
 grep second *


 28. Найти строки в файлах где есть комбинация букв “second” в любом регистре в текущей папке
 grep -i second *.*
 grep -i second *

 29. Найти строки в файлах где есть комбинация букв “second” во всех папках ниже уровнем
 grep -i "second" */*.*
 # правильно, но - маска :)
 grep -r "second" *

 30. Найти только путь и название файла в строках которого есть комбинация букв “second” в текущей папке
 grep -l 'second' *

 31. Найти все строки во всех файлах где нет комбинации “second”
 grep -v "second" *

 32. Найти только название и путь к файлам где нет комбинации “second”
 grep -L 'second' *


 33. Вывести в терминал 4 последние строки любого текстового файла
 tail -n 4 tf_2.txt

 34. Вывести в терминал 4 первые строки любого текстового файла.
 head -n 4 tf_2.txt

 35. Команда в одну строку. Создать папку и создать текстовый файл с содержимым.
mkdir inner_dir_2 && echo "it works!" > inner_dir_2/tf_6.txt

 36. Команда в одну строку. Переместить в любую одну папку текстовые файлы у которых в содержимом есть слово “sec”
grep -l sec *.* | xargs -I% mv % inner_dir_2

 37. Команда в одну строку. Скопировать в любую одну папку текстовые файлы у которых в содержимом есть слово “sec”
 grep -lR sec * | xargs -I% cp % .

 38. Команда в одну строку. Найти все строки c “sec” во всех текстовых файлах, скопировать и вставить эти строки в один новый созданный текстовый файл.
grep -h sec **/*.* > tf_7.txt

grep -rh sec . > tf_7.txt

 39. Команда в одну строку. Удалить текстовые файлы у которых в содержимом есть слово “sec”
grep -l sec **/*.* | xargs -I% rm %

grep -rl sec * | xargs rm

 40. Просто вывести в терминал строку “Good job!!”
echo "Good Job!!!"
